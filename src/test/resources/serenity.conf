environment = local

serenity {
  project.name = "Demo Project using Serenity and Cucumber"
  test.root = "com.learn.serenity.cucumber"
  tag.failures = "true"
  linked.tags = "issue"
  restart.browser.for.each = scenario
  logging = verbose
  screenshooter = shutterbug1x
}

environments {
  local {
    home.page = "http://todomvc.com/examples/angularjs/#/"
  }
  prod {
    home.page = "http://todomvc.com/examples/angularjs/#/"
  }
}
